# 준픽스 홈페이지 관리자 사용 안내

홈페이지 내용은 제작업체 없이 직접 수정할 수 있습니다.
휴대폰에서도 사용할 수 있지만, 여러 항목을 한 번에 고칠 때는 PC 가 편합니다.

## 1. 로그인

1. 홈페이지 맨 아래 "관리자" 링크를 누르거나 주소창에 `https://내도메인/admin` 을 입력합니다.
2. 전달받은 아이디와 비밀번호로 로그인합니다.
3. 로그인은 7일간 유지됩니다. 공용 PC 에서는 사용 후 "로그아웃"을 눌러 주세요.

비밀번호 변경은 서버의 `.env` 파일에서 `ADMIN_PASSWORD` 값을 바꾸고 재배포하면 됩니다 (DEPLOY.md 참고).
잊어버린 경우도 같은 방법으로 새로 정하면 됩니다.

## 2. 화면 구성

상단 탭으로 이동합니다.

| 탭 | 하는 일 |
|---|---|
| 상담 접수 | 홈페이지 상담 신청 폼으로 들어온 문의 확인, 상태 변경, 메모 |
| 시공사례 | 현장 사진과 작업 내용 등록·수정·삭제 |
| 서비스 안내 | 서비스 카드와 상세 안내(증상·원인·점검·작업방법·장비) 수정, 순서·표시 여부 |
| 작업비용 | 가격표 행 추가·삭제·순서 변경·표시 여부 |
| 고객후기 | 후기 등록·수정·삭제·표시 여부 |
| 사진 관리 | 작업차량·장비·현장 사진 갤러리, 업로드된 파일 보관함 |
| 기본 정보 | 전화번호, 카톡·블로그 주소, 지역, 로고·메인 이미지, 알림 메일, 검색·광고 설정 |

저장 버튼이 있는 화면(작업비용, 기본 정보)은 **저장을 눌러야** 홈페이지에 반영됩니다.
나머지는 등록·수정 즉시 반영됩니다.

## 3. 시공사례 올리기 (가장 자주 쓰는 기능)

현장에서 **작업 전 사진 → 작업 중 사진 → 작업 후 사진** 순서로 찍어 두면 등록이 쉽습니다.

1. "시공사례" 탭 → "새 시공사례 등록"
2. 제목: 지역과 작업 내용을 한 줄로. 예) `수원 영통구 아파트 욕실 하수구 막힘 해결`
3. 서비스 종류를 고르면 해당 서비스 안내의 "실제 시공사례"에도 함께 나옵니다.
4. 작업 지역, 작업 날짜
5. 고객 증상 / 문제 원인
6. **작업 전 사진**: "사진 선택 / 촬영"을 누르면 휴대폰에서는 카메라 또는 앨범을 고를 수 있습니다.
7. 작업 과정 (글) / 작업 과정 사진 (선택, 최대 6장)
8. 사용 장비
9. **작업 후 사진**
10. 해결 결과
11. 같은 사례를 네이버 블로그에 올렸다면 글 주소를 붙여 넣습니다.
12. "대표 사례로 표시"를 켜면 시공사례 맨 위의 **작업 전·후 비교 슬라이더**에 사용됩니다. (전·후 사진이 모두 있어야 합니다)
13. "등록하기"

사진은 올릴 때 자동으로 크기가 줄어들므로(최대 1600px) 휴대폰 원본을 그대로 올려도 됩니다.
한 장에 10MB 를 넘으면 올라가지 않습니다.

시공사례가 하나도 없으면 홈페이지에는 "현장 사례를 준비 중입니다" 안내와 블로그 버튼이 표시됩니다.

## 4. 가격표 수정 (작업비용 탭)

- **행 추가**: 표 오른쪽 위 "행 추가" → 항목명, 가격, 설명 입력
- **행 삭제**: 행 오른쪽 휴지통 아이콘
- **순서 변경**: 위/아래 화살표
- **기본 비용 ↔ 현장 견적 표 이동**: 좌우 화살표 아이콘
- **잠시 숨기기**: "홈페이지에 표시" 스위치를 끄면 삭제하지 않고 숨길 수 있습니다.
- 비고 칸에 적은 내용은 항목 아래 작은 글씨로 표시됩니다. 예) `배관 길이에 따라 추가 비용`
- 가격은 자유롭게 적습니다. `50,000원~`, `현장 견적`, `문의` 등.

수정 후 **반드시 오른쪽 위 "저장"**을 누르세요. 저장하지 않고 나가면 변경 내용이 사라집니다.
가격표 아래 ※ 안내 문구(현장 상황에 따라 비용이 달라질 수 있음)는 항상 표시됩니다.

## 5. 서비스 안내 수정

- 목록에서 연필 아이콘으로 수정합니다. 증상·원인·작업 방법·장비는 **한 줄에 하나씩** 적습니다.
- 대표 사진을 올리면 서비스 카드와 상세 안내 상단에 표시됩니다. 실제 작업 사진을 권장합니다.
- "표시" 스위치를 끄면 홈페이지에서 숨겨집니다 (삭제 아님).
- 분류를 "에어컨"으로 두면 하수구·배관 카드와 분리된 에어컨 영역에 표시됩니다.
- 서비스를 삭제해도 연결된 시공사례는 남습니다.

## 6. 고객후기

- 고객이 문자·카톡·리뷰로 남긴 내용을 옮겨 적습니다. 이름은 `김*수` 처럼 일부만 적는 것을 권장합니다.
- 후기가 하나도 없으면 홈페이지의 후기 영역은 자동으로 숨겨집니다.

## 7. 사진 관리

- **업체 사진 갤러리**: 작업 차량, 장비, 현장 사진을 올리면 "준픽스 소개" 아래에 가로로 나열됩니다. 설명(캡션)을 넣을 수 있습니다.
- **업로드 보관함**: 지금까지 올린 모든 사진입니다. 어디에도 쓰지 않는 사진은 삭제해 용량을 정리할 수 있습니다.
  시공사례 등에서 사용 중인 사진을 지우려 하면 어디에 쓰이는지 알려주고 한 번 더 확인합니다.

## 8. 기본 정보

- **전화번호**를 바꾸면 홈페이지의 모든 전화·문자 버튼이 함께 바뀝니다.
- **카카오톡 주소**, **네이버 블로그 주소**를 비워두면 해당 버튼은 표시되지 않습니다.
  - 카카오톡: 카카오톡 채널 관리자센터의 채널 URL(`https://pf.kakao.com/...`) 또는 오픈채팅방 링크
- **서비스 가능 지역 목록**: 한 줄에 한 지역씩. "서비스 가능지역" 영역에 목록으로 표시됩니다.
- **사업자등록번호, 주소**: 비워두면 표시되지 않습니다.
- **로고**: 비워두면 JUNFIX 글자 로고가 표시됩니다. 가로로 긴 투명 배경 PNG 를 권장합니다.
- **메인 화면 이미지**: 첫 화면 오른쪽 사진. 작업 차량이나 현장 사진을 권장합니다.
- **알림 받을 이메일**: 상담 신청이 들어오면 이 주소로 메일이 갑니다. (서버에 메일 발송 설정이 되어 있어야 합니다. DEPLOY.md 참고)

수정 후 오른쪽 위 "저장"을 누르세요.

## 9. 검색 등록 · 광고 전환 측정

"기본 정보" 탭 맨 아래에 붙여 넣습니다. 모르면 비워둬도 홈페이지는 정상 동작합니다.

| 항목 | 어디서 받나 | 어떤 값을 넣나 |
|---|---|---|
| 홈페이지 주소 | 도메인 | `https://내도메인` |
| 네이버 서치어드바이저 인증 코드 | searchadvisor.naver.com → 사이트 등록 → HTML 태그 | `content="..."` 안의 값만 |
| 구글 서치콘솔 인증 코드 | search.google.com/search-console → HTML 태그 | `content="..."` 안의 값만 |
| 구글 애널리틱스 4 측정 ID | analytics.google.com → 관리 → 데이터 스트림 | `G-XXXXXXXXXX` |
| 구글 태그 매니저 ID | tagmanager.google.com | `GTM-XXXXXXX` (선택) |
| 네이버 애널리틱스 ID | analytics.naver.com → 사이트 등록 | 발급된 ID (선택) |
| 구글 애즈 전환 | ads.google.com → 목표 → 전환 → 태그 설정 | `AW-숫자/라벨` 형태의 send_to 값 (선택) |

값을 넣고 저장하면 바로 적용됩니다. 사이트맵 주소는 `https://내도메인/sitemap.xml` 입니다.
서치어드바이저·서치콘솔에 사이트맵을 제출하세요.

자동으로 기록되는 이벤트:

| 이벤트 이름 | 언제 |
|---|---|
| `call_click` | 전화 버튼 클릭 |
| `sms_click` | 문자 문의 버튼 클릭 |
| `kakao_click` | 카카오톡 버튼 클릭 |
| `blog_click` | 블로그 링크 클릭 |
| `inquiry_submit` | 상담 신청 폼 완료 |

구글 애널리틱스 → 관리 → 이벤트에서 위 이벤트를 "주요 이벤트(전환)"로 표시하면 광고 성과 보고서에서 볼 수 있습니다.
구글 애즈 전환 값을 넣으면 위 이벤트가 발생할 때 애즈 전환도 함께 전송됩니다.

## 10. 자주 묻는 문제

- **수정했는데 홈페이지에 안 보여요**: 작업비용·기본 정보는 "저장"을 눌렀는지 확인. 브라우저 새로고침.
- **사진이 안 올라가요**: 10MB 이하 JPG/PNG/HEIC 만 가능합니다. 휴대폰 사진은 대부분 문제없습니다. 인터넷이 느리면 시간이 걸립니다.
- **상담 신청 알림 메일이 안 와요**: "기본 정보 → 알림 받을 이메일"이 비어 있지 않은지, 서버 메일 설정(DEPLOY.md 의 SMTP)이 되어 있는지 확인. 접수 자체는 "상담 접수" 탭에 항상 남습니다.
- **로그인이 안 돼요**: 아이디·비밀번호 확인. 비밀번호를 잃어버렸으면 서버 `.env` 의 `ADMIN_PASSWORD` 를 바꾸고 재배포.
- **전·후 비교 슬라이더가 안 보여요**: "대표 사례"로 표시된 사례에 작업 전·후 사진이 모두 있어야 합니다.
